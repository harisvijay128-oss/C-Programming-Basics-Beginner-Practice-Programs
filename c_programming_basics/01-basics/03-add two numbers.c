#include <stdio.h>

int main()
{
  int a, b, sum;

  printf("Enter two integers: ");
  scanf("%d %d", &a, &b);

  // sum of two integer variables
  sum = a + b;

  printf("\nSum of a and b is %d", sum);

  return 0;
}
